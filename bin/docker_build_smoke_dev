#!/bin/bash
set -e

cd "$(dirname "$0")"/..

# Local architecture build only.
# Does not push the docker image.
direnv exec . make docker-build && direnv exec . make docker-smoke PYTHON="$PWD/.venv/bin/python"
